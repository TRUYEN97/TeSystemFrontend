export enum ROUTE {
  LOGIN = "/login",
  SIGNUP = "/signup",

  HOME = "/",

  USERS = "/users",
  USER_ID = "/users/:id",
  NEW_USER = "/users/new",

  TEAMS = "/teams",
  TEAM_ID = "/teams/:id",
  NEW_TEAM = "/teams/new",

  LOCATIONS = "/locations",

  NOT_FOUND = "/not-found",
}
